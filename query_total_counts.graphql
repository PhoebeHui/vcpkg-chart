query total_counts {
    rateLimit {
        cost
        limit
    }
    repository(owner: "microsoft", name: "vcpkg") {
        pullRequests(first: 0) {
            totalCount
        }
        issues(first: 0) {
            totalCount
        }
    }
}
